# Default values for mysql.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: redis
  tag: "latest"

volume:
  storage: 2Gi
  hostPath: /Users/gengxiankun/Variable/lib/redis

requests:
  volume:
    storage: 2Gi

service:
  type: LoadBalancer
  port: 6379
  targetPort: 6379
